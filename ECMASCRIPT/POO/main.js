
/*Abstração

class Cadeira {
    constructor(quantidade_pernas, giratoria, cor) {
        this.quantidade_pernas = quantidade_pernas
        this.giratoria = giratoria
        this.cor = cor
    }

    girar_cadeira() {
        if (giratoria){
            console.log('Girando em 360°')
        } else {
            console.log('Não é uma cadeira giratoria.')
        }
    }
}

let cadeira = new Cadeira(3, true, 'azul')
cadeira.girar_cadeira()

class Sofa {
    
    constructor(cor, valor, quantidade_acentos) {
        this.cor = cor
        this.valor = valor
        this.quantidade_acentos = quantidade_acentos

    }
}



class Calculadora {
    constructor(a, b) {
        this.a = a
        this.b = b
    }

    calcular() {
        return this.a + this.b
    }
}


let calc = new Calculadora(1, 2)
console.log(calc.calcular())


Polimorfismo


A convenção para indicar que um atributo é privado é: '_'atributo

para se trabalhar com esses atributos, usasse getters e setters.


ex: get atributo() {return atributo}
    set atributo(param) {atributo = param}


*/